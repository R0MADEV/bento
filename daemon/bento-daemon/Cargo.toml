[package]
name = "bento-daemon"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "bento-daemon"
path = "src/main.rs"

[dependencies]
bento-core = { path = "../bento-core" }
tokio = { version = "1", features = ["rt-multi-thread", "net", "io-util", "macros", "sync"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
# Phone remote: HTTP + WebSocket server that bridges terminals to the browser.
axum = { version = "0.7", features = ["ws"] }
futures-util = "0.3"
getrandom = "0.2"
