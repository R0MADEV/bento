name = "bento-signaling"
main = "src/worker.ts"
compatibility_date = "2026-08-23"

# Created once via `npm run kv:create` — paste the returned id here.
[[kv_namespaces]]
binding = "PAIRING"
id = "REPLACE_WITH_KV_NAMESPACE_ID"
